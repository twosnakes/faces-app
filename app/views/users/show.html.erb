<div id="app">
  <div class="well" id="svg-top">
    <h1>SVG CANVAS</h1> 
    <h1>Hi <%= current_user.first_name%> <%= current_user.last_name %>!</h1>                   
    <div>
      <%= link_to "New Project", "/users/#{@user.id}/projects", method: :post, class: "btn btn-warning" %> 
<!--       <li class="btn btn-info" v-on:click="saveFace">Save Project</li>
      <button class="btn btn-success" onclick="exportPNG();">Export</button>   
 -->
    </div>
  </div>
</div>