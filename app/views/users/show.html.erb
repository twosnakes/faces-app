<h1>SVG CANVAS</h1>

        <button onclick="window.location.href='/users/<%= current_user.id %>/projects'">Saved Projects</button>
                <h1>Hi <%= current_user.first_name%> <%= current_user.last_name %>!</h1>
               

                           
                            
                      <div>
                        <li>Make Face Library</li>
                        <li>Face Color Library</li>
                        <li>Eye Library</li>
                        <li>Nose Library</li>
                        <li>Mouth Library</li>
                      </div>   

                      if project.completed != true    
                            <button onclick="window.location.href='???'">Finalize/Share</button>

                            <button onclick="window.location.href='/users/#{params[:user_id]}/projects/:id">Scrap Project</button>
                            <button onclick="window.location.href='/users/#{params[:user_id]}/projects/new'">New Project</button>
                            <%= link_to "New Project", "/users/#{@user.id}/projects", method: :post %>
                            <button onclick="window.location.href='/users/#{params[:user_id]}/projects'">Save In Project</button>